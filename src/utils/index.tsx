import { onDragEnd } from "./on-drag-end";
import { saveTask } from "./save-task";
import { data } from "./test-data";
import { 
    TaskData, 
    TaskComponent, 
    ColumnData, 
    BoardData, 
    TaskModalProps,
} from "./types";

export {
    onDragEnd,
    saveTask,
    data
}
export type {
    TaskData,
    TaskComponent,
    ColumnData,
    BoardData,
    TaskModalProps
}