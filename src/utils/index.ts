import { setLocalStorage, getLocalStorage } from "./local-storage";
import { boardData } from "./board-data";
import { 
    TaskData, 
    TaskComponent, 
    ColumnData, 
    ColumnComponent,
    BoardData, 
    TaskModalProps,
} from "./types";

export {
    boardData,
    setLocalStorage,
    getLocalStorage,
}
export type {
    TaskData,
    TaskComponent,
    ColumnData,
    ColumnComponent,
    BoardData,
    TaskModalProps
}